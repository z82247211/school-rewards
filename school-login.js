window.addEventListener('load',function(){const btn=document.getElementById('loginBtn');const sc=document.getElementById('schoolCode');const u=document.getElementById('username');const p=document.getElementById('password');const msg=document.getElementById('loginMessage');if(!btn||!sc)return;async function schoolLogin(){const school_code=sc.value.trim().toUpperCase(),username=u.value.trim(),password=p.value;if(!school_code||!username||!password){msg.textContent='کد مدرسه، نام کاربری و رمز عبور را وارد کنید.';return}try{msg.textContent='در حال ورود...';const r=await fetch('https://ubjsvqhxoufdjwzqkmsd.supabase.co/functions/v1/school-api',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({action:'login',school_code,username,password})});const d=await r.json();if(!r.ok)throw Error(d.error||'نام کاربری، رمز عبور یا کد مدرسه اشتباه است.');sessionStorage.setItem('schoolToken',d.token);sessionStorage.setItem('schoolCode',school_code);location.reload()}catch(e){msg.textContent='❌ '+e.message}}btn.onclick=schoolLogin;[sc,u,p].forEach(x=>x.addEventListener('keydown',e=>{if(e.key==='Enter')schoolLogin()}));});